

interface ReservationProps {
    address: `0x${string}`
}

export function Reservation({ address }: ReservationProps) {
    
    // reservation nft from wallet
    return (
        <div>
            <h1>Reservation</h1>
        </div>
    )
}